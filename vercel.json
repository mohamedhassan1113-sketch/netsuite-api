{
  "version": 2,
  "builds": [
    {
      "src": "server.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/health",
      "dest": "server.js"
    },
    {
      "src": "/api/(.*)",
      "dest": "server.js"
    }
  ]
}
```

---

### **File 4: .gitignore**

Create a file named `.gitignore` and paste this:
```
node_modules/
.env
.vercel
.DS_Store